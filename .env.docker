# Docker environment overrides
# Use local Redis container instead of Upstash
REDIS_URL=redis://redis:6379

# Database URL uses service name instead of localhost
DATABASE_URL=postgresql://quizisuser:quizispass@postgres:5432/quizisdb

# CORS origin for nginx (port 80)
CORS_ORIGIN=http://localhost:80
